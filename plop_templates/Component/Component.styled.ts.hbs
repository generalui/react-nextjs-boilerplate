/**
 * Styles for component {{properCase name}}
 */
import styled from 'styled-components'

{{!-- Todo: inherit all styles from Box component rather than div --}}
export const Styled{{properCase name}} = styled.div``
