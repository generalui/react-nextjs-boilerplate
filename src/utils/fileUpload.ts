// TODO: 10mb is the free tier cloudinary limit, we might want to increase this once we're on real hardware
export const MAX_FILE_SIZE_MB = 10
export const BYTES_IN_MB = 1000 * 1000
export const MAX_FILE_SIZE_BYTES = MAX_FILE_SIZE_MB * BYTES_IN_MB
