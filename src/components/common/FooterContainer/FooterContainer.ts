import styled from 'styled-components'

/**
 * This is an example component - not intended for production use
 */
export const FooterContainer = styled.footer`
	padding: var(--spaces-5) 0;
	border-top: 1px solid var(--border-light);
`
